# Local dev only (loaded by `npm start`, overrides .env.local's REACT_APP_WESTPRINT_API).
# Requests go to the relative path below, which src/setupProxy.js forwards
# server-side to https://dev.huynhdous.com, avoiding the browser CORS block.
REACT_APP_WESTPRINT_API=/
